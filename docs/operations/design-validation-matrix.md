# Design Validation Matrix

AP-101 inventories the design surfaces that VA Auto-Pilot claims to support and
maps each one to an executable proof path. A design is not considered validated
by prose alone: each row below has either a concrete command/test entrypoint or
an explicit follow-up task that must close the runtime gap.

## Baseline Gates

Run these before claiming the matrix is current:

```bash
npm run typecheck
npm run check:all
npm run check:e2e
```

For public narrative and release-readiness rows, also run the stale wording scan
from `docs/operations/public-narrative-spec.md` and
`docs/operations/open-source-readiness-checklist.md` through
`npm run check:public-narrative`.

## Matrix

| Design surface | Source | Executable proof path | Current evidence | Gap / follow-up |
| --- | --- | --- | --- | --- |
| Human goal and intent control surface | `docs/operations/start-va-auto-pilot-prompt.md`, `docs/operations/va-auto-pilot-protocol.md`, `docs/todo/human-board.md` | `node scripts/auto-pilot.mjs goal --text "..." --json`; `node scripts/auto-pilot.mjs intent <type> --text "..."`; `node scripts/auto-pilot.mjs cockpit --json`; `node scripts/sprint-board.mjs next --strict`; `npm run check:e2e` | `check:e2e` scenario 05 proves pending human-board instructions are injected into worker prompts; current validation goal was captured through the intent/cockpit path. | No AP-101 gap. Keep future strategic direction flowing through intent commands, not raw board edits. |
| Sprint board state machine and pitfall lifecycle | `docs/operations/va-auto-pilot-protocol.md`, `docs/todo/sprint.md`, `.va-auto-pilot/sprint-state.json` | `node scripts/sprint-board.mjs summary`; `node scripts/sprint-board.mjs next --json`; `node scripts/sprint-board.mjs update --id <task>`; `node scripts/sprint-board.mjs pitfall ...`; `npm run check:cli-flows`; `npm run check:e2e` | `check:e2e` scenarios 01-04 and 09-10 cover Backlog/In Progress/Failed/Done, retry, triple-fail stop, pitfall creation, and sprint completion; `check:cli-flows` covers sprint-board and pitfall CLI flows. | No AP-101 gap. |
| Orchestrated manager loop | `docs/operations/start-va-auto-pilot-prompt.md`, `docs/operations/va-auto-pilot-protocol.md` | `node scripts/auto-pilot.mjs orchestrate init`; `plan`; `review-plan`; `approve-plan`; `dispatch`; `observe --json`; `await-workers`; `approve-commit`; `commit`; `journal`; `npm run check:cli-flows` | AP-102 dogfooded the full governed path with a deterministic scoped worker and retained observe JSON, journal, gate output, evidence paths, task commit, journal control commit, and clean temp git status. `test-flows/orchestrate-cli.yaml` and unit tests cover empty plan, review/approval gates, dry-run await-workers, observe snapshots, stale checkpoint blocks, governed commit, unattended replan after journal commits, commit-failure approval clearing, and controlled terminal states. | No AP-102 gap. Keep future orchestrate phase changes covered by a governed CLI flow and observable evidence bundle. |
| Governance checkpoints and stale approval invalidation | `docs/operations/governance-checkpoints.md`, `scripts/lib/orchestration-state.mjs`, `scripts/lib/orchestration-cli.mjs` | `node scripts/auto-pilot.mjs orchestrate review-plan --dry-run --json`; `approve-plan --json`; `dispatch --json`; `approve-commit --tasks <ids> --json`; `node scripts/auto-pilot.mjs observe --json`; `npm run check:units`; `node scripts/test-cli-flows.mjs --flow test-flows/orchestrate-cli.yaml` | Runtime flow now proves plan-review gating plus `STALE_CONTEXT` dispatch blocks for sprint-state, human-board, and git HEAD drift, with `checkpoint.stale` events written to `.va-auto-pilot/evidence/events.jsonl`. Unit tests still cover checkpoint construction, review requirements, dispatch approval requirements, governance events, close, and observe snapshot output. | No AP-105 governance gap. Keep every new approval surface bound to checkpoint hashes before dispatch. |
| Observability events and evidence bundles | `docs/operations/observability-spec.md`, `schemas/observability-event.schema.json`, `schemas/evidence-bundle.schema.json`, `docs/operations/observability-examples/` | `npm run check:observability`; `node scripts/generate-observability-examples.mjs`; `node scripts/test-cli-flows.mjs --flow test-flows/orchestrate-cli.yaml`; `node scripts/test-cli-flows.mjs --flow test-flows/cycle-restart.yaml`; `npm run check:all` | Runtime loop execution now writes `task.started`, `task.command`, `task.gate`, `task.completed`, and `task.failed` events to `.va-auto-pilot/evidence/events.jsonl`, materializes per-task evidence bundles under `.va-auto-pilot/evidence/<runId>/<taskId>/`, and writes redacted manifests. The governed orchestrate flow proves checkpoint `eventLogPath`/`evidenceBundleDir`, task bundle files, event log entries, and clean committed evidence. The cycle-restart flow proves normal loop auto-commit tracks event logs and two task bundle manifests. `check:observability` still validates completed/failed examples. | No AP-104 gap. Keep future runtime events mirrored into bundle generation before task commits. |
| Loop cycle-boundary journal cleanliness | `scripts/auto-pilot-loop.mjs`, `docs/todo/run-journal.md`, `test-flows/cycle-restart.yaml`, `test-flows/auto-pilot-loop-cli.yaml` | `node scripts/test-cli-flows.mjs --flow test-flows/cycle-restart.yaml`; `node scripts/test-cli-flows.mjs --flow test-flows/auto-pilot-loop-cli.yaml`; `npm run check:all` | The loop now commits the final cycle-boundary journal entry before exit when commits are enabled and the journal is the only remaining dirty file, including task-completion exits and no-task journal-only exits. `cycle-restart.yaml` proves restart, `--single-cycle`, and `--max-cycles 1` runs finish with `WORKTREE=clean`; `auto-pilot-loop-cli.yaml` proves a normal parallel commit-enabled run finishes with `worktree=0`. | No AP-108 gap. Do not let future cycle-boundary bookkeeping append after the final commit without a matching clean-worktree assertion. |
| va-agent-protocol fixture compatibility | `docs/operations/protocol-fixtures.md`, `docs/operations/protocol-fixtures/*.json`, `schemas/protocol-fixture.schema.json` | `npm run check:protocol-fixtures`; `npm run check:all` | `check:protocol-fixtures` validates fixture JSON, `trackToTaskUnit()` mapping, evidence manifest schema, required events, gate status, and failed-gate mapping. | No AP-101 gap unless protocol fields change. |
| Permission scope and worker boundaries | `docs/operations/permission-scope.md`, `schemas/permission-scope.schema.json`, `scripts/lib/permission-scope.mjs` | `npm run check:units`; dispatch through `node scripts/auto-pilot-loop.mjs`; governed dispatch through `node scripts/auto-pilot.mjs orchestrate await-workers --no-colony`; `node scripts/test-cli-flows.mjs --flow test-flows/orchestrate-cli.yaml` | Runtime dispatch now emits worker `task.command` events with permission decisions, blocks destructive worker commands before spawn, and fails successful workers that modify files outside the task permission scope before they can enter Review. The governed CLI flow captures the worker prompt and asserts the `## Permission Scope` block, out-of-scope file failure, destructive command non-execution, journal signal, and event-log permission decisions. | No AP-105 permission gap. Keep custom worker permissions explicit on tasks that need generated side artifacts. |
| Quality gate configuration, adaptive gates, and gate trust | `docs/operations/va-auto-pilot-protocol.md`, `docs/operations/quality-gate-examples.md`, `.va-auto-pilot/config.yaml`, `scripts/auto-pilot-gates.mjs` | `npm run check:all`; `npm run check:runtime-proof`; `node scripts/auto-pilot.mjs gates audit --json`; `node scripts/auto-pilot.mjs gates maintain --apply --json`; the review gate command configured in `.va-auto-pilot/config.yaml` | `check:all` runs typecheck, build-style checks, CLI flows, observability, MCP resources, runtime proof, protocol fixtures, public narrative scan, and distribution validation; unit tests cover gate inference, adaptive gate maintenance, weak gate trust, and disabled smoke maintenance notes. CI runs `typecheck`, `check:all`, and `check:e2e`. | No AP-107 gateTrust gap. Current config has no missing required gates, no weak required placeholder commands, and gateTrust explains disabled smoke through `maintenanceNotes`. |
| Smoke-test runner | `.va-auto-pilot/config.yaml`, `scripts/smoke-test-runner.mjs`, `scripts/lib/project-gates.mjs`, `scripts/validate-runtime-proof.mjs` | `npm run check:runtime-proof`; `node scripts/smoke-test-runner.mjs --config <path>`; `npm run check:units`; `node scripts/auto-pilot.mjs gates audit --json` | Unit tests cover disabled smoke, missing section, empty critical paths, path escape, missing Puppeteer, bad JSON, empty output, failure details, and successful smoke output. `check:runtime-proof` now proves the current disabled path, an enabled path using a deterministic runner result, and the real smoke-test-runner invalid-config failure path. | No AP-107 smoke gap. Keep smoke disabled until real critical paths exist, and keep the disabled reason in gateTrust maintenance notes. |
| Budget guardrails | `docs/operations/budget-guardrails.md`, `scripts/lib/budget-guardrails.mjs`, `scripts/auto-pilot-loop.mjs` | `npm run check:units`; `node scripts/test-cli-flows.mjs --flow test-flows/auto-pilot-loop-cli.yaml`; `node scripts/auto-pilot-loop.mjs --max-cycles <n>` with budget config fixture | Unit tests cover flat/nested budget normalization, warn/stop semantics, command/token summary formatting, usage extraction, and loop hard-stop journaling. The loop CLI flow now runs a real config with `maxCyclesSoft: 1` and `maxCyclesHard: 2`, asserts JSON budget states, and verifies `budget=warn`/`budget=stop` journal evidence. | No AP-106 budget gap. Keep budget decisions journaled at cycle boundaries. |
| Eval gates and eval history | `docs/operations/eval-gates.md`, `scripts/lib/project-gates.mjs`, `scripts/lib/eval-history.mjs` | `npm run check:units`; `node scripts/test-cli-flows.mjs --flow test-flows/auto-pilot-loop-cli.yaml`; `node scripts/sprint-board.mjs eval-compare`; configured `qualityGate.evalCommand` or `qualityGate.evalGates[]` | Unit tests cover PASS/FAIL/AMBIGUOUS parsing, required eval blocking, advisory eval failures, JSONL history, and `eval-compare` text/JSON summaries. The loop CLI flow now runs PASS, FAIL, and ambiguous eval commands through real task transitions, verifies `.va-auto-pilot/evidence/eval-history.jsonl`, confirms ambiguous output blocks Done, and checks `eval-compare --json`. | No AP-106 eval gap. Required evals must keep treating ambiguous output as non-passing. |
| MCP read-only resources | `docs/operations/mcp-readonly-resources.md`, `scripts/lib/mcp-readonly-resources.mjs`, `scripts/validate-runtime-proof.mjs` | `npm run check:mcp-resources`; `npm run check:runtime-proof`; `npm run check:units`; `node scripts/auto-pilot.mjs observe --json` for orchestration snapshot refresh | `check:mcp-resources` validates descriptor metadata, read-only payloads, sprint summary dispatch authority, pitfall filtering, orchestration snapshot payload shape, and fail-closed unknown URI behavior. `check:runtime-proof` reads every listed resource against the current repo state, parses JSON payloads, and reasserts sprint-summary is not dispatch authority. | No AP-107 MCP gap. Keep current-repo resource reads in `check:runtime-proof` when adding new resources. |
| E2E harness | `e2e/README.md`, `e2e/scenarios/*.yaml`, `e2e/run-e2e.mjs` | `npm run check:e2e`; `node e2e/run-e2e.mjs --scenario <file>` | Current full run passes 10/10 scenarios: state transitions, fail/retry, triple fail, pitfall lifecycle, human-board injection, quality gate sequence, parallel dispatch, sprint completion, and demo lifecycle. AP-102 separately proves the governed manager approval path, so deterministic e2e and governed dogfood now cover complementary runtime layers. | No AP-102 E2E gap. Keep deterministic e2e for repeatable state-machine coverage and governed flows for manager approval semantics. |
| LLM quality observation system | `e2e/quality/README.md`, `e2e/quality/run-quality.mjs`, `e2e/quality/scenarios/*.yaml` | `npm run check:quality`; `node e2e/quality/run-quality.mjs --all --no-judge`; `node e2e/quality/run-quality.mjs --trend` | CLI entrypoint exists and prints help. Full judged runs require `ANTHROPIC_API_KEY` or compatible `ANTHROPIC_BASE_URL`; `docs/operations/ci-parity.md` classifies this as release/manual evidence rather than normal deterministic CI. | AP-106 covers eval/quality history so ambiguous or missing judge output cannot masquerade as pass evidence. |
| CI parity | `.github/workflows/ci.yml`, `package.json`, `docs/operations/ci-parity.md` | GitHub Actions: `npm ci`, `npm run typecheck`, `npm run check:all`, `npm run check:e2e`; PR doc-store job: `npm run doc-store:doctor`, `npm run doc-store:enforce-staged -- --base main` | CI now exposes typecheck as a separate workflow step and `check:all` includes typecheck, public narrative scan, deterministic tests, observability, MCP resources, runtime proof, protocol fixtures, and distribution validation. `docs/operations/ci-parity.md` records CI-required versus release/manual gates. | No AP-103 gap. Keep this row current when adding package scripts or GitHub Actions jobs. |
| Distribution and package contents | `docs/operations/distribute-skill.md`, `docs/operations/open-source-readiness-checklist.md`, `package.json`, `scripts/validate-distribution.mjs` | `npm run validate:distribution`; `npm pack --dry-run --json`; clean temp install smoke from the readiness checklist | `validate:distribution` is included in `check:all` and validates required package files, forbidden package paths, scaffold files, dependencies, scripts, website metadata, and source package assets. Clean install smoke remains release/manual evidence in `docs/operations/ci-parity.md`. | No AP-103 gap. |
| Public narrative and positioning checks | `docs/operations/public-narrative-spec.md`, `docs/operations/public-positioning-audit.md`, `docs/operations/open-source-readiness-checklist.md`, `scripts/validate-public-narrative.mjs` | `npm run check:public-narrative`; `npm run check:all` | The stale-expression scan is now a Node package script with the documented exclusions and is included in `check:all`, so CI runs it through the check job. | No AP-103 gap. |
| DocStore PR hygiene | `.github/workflows/ci.yml`, `scripts/doc-store-cli.mjs`, `scripts/test-doc-store*.mjs` | `npm run check:doc-store`; `npm run check:doc-store-mode`; `npm run doc-store:doctor`; `npm run doc-store:enforce-staged -- --base main` | `check:all` includes doc-store unit/mode tests; CI adds the PR-only doctor and enforce-staged job. | No AP-103 gap. |

## Backlog Coverage

- AP-102: governed orchestrated dogfood run. Closed by the deterministic
  governed orchestrate flow, observe/journal/gate evidence retention, task
  commit, journal control commit, and clean temp git status.
- AP-103: CI parity, release/manual gates, and public scan policy. Closed by
  `docs/operations/ci-parity.md`, `npm run check:public-narrative`, and CI
  typecheck coverage.
- AP-104: real observability/evidence bundle proof. Closed by runtime task
  events, per-task bundle materialization, and CLI flow assertions in
  `orchestrate-cli.yaml` and `cycle-restart.yaml`.
- AP-105: governance checkpoints plus permission scope runtime proof. Closed by
  stale checkpoint runtime flows for sprint-state/human-board/git-head drift
  and permission enforcement for prompt injection, out-of-scope diffs, and
  destructive command blocking.
- AP-106: budget guardrails plus eval/quality history runtime proof. Closed by
  loop CLI flows for budget soft/hard cycle limits and runtime eval
  PASS/FAIL/AMBIGUOUS history.
- AP-107: MCP resources plus smoke-test runner proof. Closed by
  `npm run check:runtime-proof` and its current-repo MCP reads, smoke
  enabled/disabled/invalid-config paths, gateTrust disabled-smoke explanation,
  and required-placeholder audit.
- AP-108: final cycle-boundary journal dirty-state proof. Closed by
  commit-enabled loop exits that commit journal-only boundary entries and by
  clean-worktree assertions in `cycle-restart.yaml` and
  `auto-pilot-loop-cli.yaml`.

Rows without a follow-up task already have deterministic commands included in
`npm run check:all`, `npm run check:e2e`, or an explicitly documented release
scan. If that changes, add a backlog item before marking the affected design
validated.
